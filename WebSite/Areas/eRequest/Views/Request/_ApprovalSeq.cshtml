@model List<Core.Entities.RequestLoopFlow>
@if (Model.Count > 0)
{
    foreach (var item in Model)
    {
        <button class="btn @item.StatusClass btn-approval" data-id="@item.RequestLoopFlowID">@item.PhaseName</button> 
    }
}

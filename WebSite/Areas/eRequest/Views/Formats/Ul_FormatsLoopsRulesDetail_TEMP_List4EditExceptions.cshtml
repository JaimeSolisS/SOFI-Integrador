@model List<Core.Entities.FormatsLoopsRulesDetail_TEMP>
@if (Model.Count > 0)
{
    <br />
    <b>@Resources.Request.lblExceptions:</b>
    <ul>
        @foreach (var item in Model)
        {
            <li>
                @Html.Raw(item.Description)
            </li>
        }
    </ul>
}
